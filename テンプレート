スレッド一覧表示（標準）
	検索窓 キーワード
	スレッドを投稿するやつ（別のページに飛んでやる？）
		アイコン選択
		名前
		タイトル
		内容	//Null可
	<table>で
		（番号振る？）
		アイコン
		名前
		タイトル
		日付
		いいね		//初期値0
		閲覧数		//初期値0
		（コメント数）列の数数えればいける？
	（ある程度の件数でページ変えたい）

スレッド一覧表示（閲覧数順）
	中身は同じ
	
スレッド一覧表示（いいね順）
	中身は同じ
	
スレッド一覧表示（検索文字列を含む）
	中身は同じ
	
スレッドに対する返答表示
	<table>でスレッド
		アイコン
		名前
		タイトル
		日付
		閲覧数		//初期値0
		内容		//NotNullにしないといけなかった
		いいね		//初期値0
	スレッドのいいねボタン
	（コメント数）DBの列増やさないと無理そう
	スレッドに対する返答投稿（別のページに飛ぶ？）
		アイコン選択
		名前
		内容
	<table>で返答
		（番号振る？）
		アイコン
		名前
		日付
		内容
	それぞれのコメントに対してのいいねボタン（できるかはわからない）
	（ある程度の件数でページ変えたい）
